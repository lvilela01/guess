# First project with React + Vite + Typescript + Node.js

This is the first React + Vite project developed during classes at Rocketseat. The goal is to put the fundamentals of React into practice, such as creating components, using `useState` and `useEffect`, and styling individual components as well as the entire application (global.css).
